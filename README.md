# testing_jest_node_typeScript_pg_typeORM

* `docker-compose up -d --build`
* Go to each project(template-micros and template-middleware) and run `make install` and `make run-local`

# test
* npm run test
